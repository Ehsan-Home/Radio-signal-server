# Radio-signal-server ⚡️

## How to run? 📦
- Build the docker
```
docker build -t img .
```
- Run the image
```
docker run -it img
```

## Output 🖼️
It will print a randomized radio object as well as starting the Django project.
